---
title: "Introduction"
description: "Official API documentation for UGC Inc - Automate TikTok and Instagram content creation"
---

## Welcome to UGC Inc API

The UGC Inc API allows you to programmatically manage social media accounts, create and schedule posts, track statistics, and automate account warming tasks for TikTok and Instagram.

## Base URL

All API requests should be made to:

```
https://api.ugc.inc
```

## Authentication

All API endpoints require authentication using an API key. Include your API key in the request headers:

```bash
Authorization: Bearer YOUR_API_KEY
```

### Getting Your API Key

To access accounts and receive an API key:

1. Visit [ugc.inc](https://ugc.inc)
2. **Schedule a call** with our team
3. After your call, you'll receive access to accounts and your API key

<Warning>
  **Keep your API key secure!** Never commit it to version control or expose it in client-side code.
</Warning>

## Installation

### NPM Package (Recommended)

We provide an official TypeScript/JavaScript client with full type safety:

```bash
npm install ugcinc
```

```typescript
import { UGCClient } from 'ugcinc';

const client = new UGCClient({
  apiKey: 'your-api-key-here'
});
```

### Direct API Calls

You can also make direct HTTP requests to the API endpoints using any HTTP client.

## Response

All API responses follow a consistent, type-safe format that makes error handling straightforward.

### Response Structure

Every API response includes an `ok` field that indicates success or failure:

**Success Response:**
```json
{
  "ok": true,
  "code": 200,
  "message": "Success",
  "data": { ... }
}
```

**Error Response:**
```json
{
  "ok": false,
  "code": 400,
  "message": "Error description"
}
```

### Using the Response Pattern

The `ok` field allows you to easily check if the request succeeded:

```typescript
const response = await client.accounts.getAccounts();

if (response.ok) {
  // Success - data is available
  console.log(response.data);
  response.data.forEach(account => {
    console.log(account.username);
  });
} else {
  // Error - handle the error
  console.error(`Error ${response.code}: ${response.message}`);
}
```

```python
response = requests.post(
    'https://api.ugc.inc/accounts',
    headers={'Authorization': 'Bearer YOUR_API_KEY'},
    json={}
)

data = response.json()

if data['ok']:
    # Success - data is available
    print(data['data'])
else:
    # Error - handle the error
    print(f"Error {data['code']}: {data['message']}")
```

### HTTP Status Codes

Common status codes you may encounter:

- **200** - Success
- **400** - Bad Request (invalid parameters)
- **401** - Unauthorized (invalid or missing API key)
- **404** - Not Found (resource doesn't exist)
- **500** - Internal Server Error

## Data Structures

The API works with several core data structures. Understanding these structures will help you effectively use the API.

### Account

Represents a social media account (TikTok or Instagram) that you can manage through the API.

```typescript
{
  id: string;              // Unique account identifier
  type: string;            // Platform type: 'tiktok' or 'instagram'
  tag: string | null;      // Custom tag for categorization
  org_group: string | null;   // Organization group identifier
  user_group: string | null;  // User group identifier
  username: string | null;    // Account username/handle
  nick_name: string | null;   // Display name
  pfp_url: string | null;     // Profile picture URL
}
```

#### Account Organization

The `org_group` and `user_group` fields allow you to organize and subdivide accounts:

**Organization Groups (`org_group`):**
- Group accounts by department, campaign, or purpose
- Examples: "marketing", "sales", "influencer_campaign_2024"
- Use for filtering accounts by business unit or project
- Multiple accounts can share the same org_group

**User Groups (`user_group`):**
- Assign accounts to specific team members or users
- Examples: "john_smith", "team_alpha", "content_creator_1"
- Use for access control and responsibility assignment
- Allows filtering accounts by assigned user/team

**Example Use Cases:**

```typescript
// Filter accounts by organization group
const marketingAccounts = await client.accounts.getAccounts({
  org_group: 'marketing'
});

// Filter accounts by user assignment
const johnsAccounts = await client.accounts.getAccounts({
  user_group: 'john_smith'
});

// Combine filters
const campaignAccounts = await client.accounts.getAccounts({
  org_group: 'holiday_campaign_2024',
  user_group: 'content_team'
});
```

### Post

Represents a video or slideshow post on a social media account.

```typescript
{
  id: string;              // Unique post identifier
  account_id: string;      // Account that created this post
  type: 'video' | 'slideshow';  // Post type
  status: string;          // Current status: 'scheduled', 'pending', 'complete', 'failed'
  social_id: string | null;     // Platform post ID (available after posting)
  caption: string | null;       // Post caption/description (max 500 chars)
  media_urls: string[] | null;  // URLs to video/image files
  music_post_id: string | null; // Music/audio track ID
}
```

### Task

Represents an account-level task such as profile updates or warmup activities.

```typescript
{
  id: string;              // Unique task identifier
  account_id: string;      // Account this task belongs to
  type: TaskType;          // Task type (see below)
  status: string;          // Current status: 'scheduled', 'pending', 'complete', 'failed'
  scheduled_time: string | null;  // When task should execute (ISO 8601)
  duration: number | null;        // Task duration in seconds (for warmup tasks)
  keywords: string | null;        // Keywords for search tasks
  edit_profile_info: object | null;  // Profile update details
  created_at: string;      // Creation timestamp
}
```

**Task Types:**
- `warmup_scroll` - Account scrolling activity to maintain authenticity
- `warmup_search_term` - Search for specific terms
- `warmup_search_profile` - Search for profiles
- `edit_profile` - Update profile (nickname, avatar, bio)
- `update_posts` - Refresh post data

### AccountStat

Represents statistics for an account at a specific point in time.

```typescript
{
  id: string;              // Unique stat record identifier
  account_id: string;      // Associated account ID
  followers: number | null;   // Follower count
  following: number | null;   // Following count
  views: number | null;       // Total content views (aggregated from posts)
  likes: number | null;       // Total likes (aggregated from posts)
  created_at: string;      // When stat was recorded (ISO 8601)
}
```

<Note>
  Account stats are created by aggregating post-level metrics. Use the `/stats/refresh` endpoint to fetch fresh statistics from the platform API.
</Note>

### PostStat

Represents engagement statistics for an individual post at a specific point in time.

```typescript
{
  id: string;              // Unique stat record identifier
  post_id: string;         // Associated post ID
  views: number | null;    // View count
  likes: number | null;    // Like count
  comments: number | null; // Comment count
  shares: number | null;   // Share count
  saves: number | null;    // Save/bookmark count
  created_at: string;      // When stat was recorded (ISO 8601)
}
```

<Note>
  Post stats are snapshots in time. Multiple stat records may exist for a single post, allowing you to track performance over time.
</Note>

## API Endpoints

The API is organized into the following categories:

### 📋 Accounts
Manage social media accounts, update profiles, and check account status.

### 📊 Statistics
Retrieve and refresh statistics for accounts and posts.

### ✅ Tasks
Manage and monitor tasks including warmup activities and profile updates.

### 📱 Posts
Create, schedule, and monitor video and slideshow posts.
