---
title: "Get Social Audio"
description: "Retrieve social audio files with optional filters"
---

## Endpoint

```
POST https://api.ugc.inc/media/social-audio
```

## Overview

Retrieve social audio files imported from TikTok. Returns only social audio, not user-uploaded media. Supports filtering by specific IDs or tags.

## Request Body

<ParamField body="ids" type="string[]">
  Filter to specific social audio IDs. Returns only audio matching these IDs.
</ParamField>

<ParamField body="tag" type="string">
  Filter social audio by tag
</ParamField>

## Response

<ResponseField name="data" type="SocialAudio[]">
  Array of social audio objects
  
  <Expandable title="SocialAudio properties">
    <ResponseField name="id" type="string">
      Unique audio identifier
    </ResponseField>
    
    <ResponseField name="org_id" type="string">
      Organization ID
    </ResponseField>
    
    <ResponseField name="name" type="string | null">
      Audio/song title
    </ResponseField>
    
    <ResponseField name="tag" type="string | null">
      Custom tag for categorization
    </ResponseField>
    
    <ResponseField name="social_post_link" type="string | null">
      Link to a TikTok post using this audio
    </ResponseField>
    
    <ResponseField name="social_audio_link" type="string | null">
      Direct link to the TikTok audio/music page
    </ResponseField>
    
    <ResponseField name="platform_type" type="'tiktok' | null">
      Source platform
    </ResponseField>
    
    <ResponseField name="preview_url" type="string | null">
      URL to cover image
    </ResponseField>
    
    <ResponseField name="audio_url" type="string | null">
      URL to audio file
    </ResponseField>
    
    <ResponseField name="created_at" type="string">
      ISO timestamp of creation
    </ResponseField>
    
    <ResponseField name="media_type" type="'social_audio'">
      Discriminator field
    </ResponseField>
  </Expandable>
</ResponseField>

<RequestExample>

```bash cURL
curl -X POST https://api.ugc.inc/media/social-audio \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "tag": "trending"
  }'
```

```python Python
import requests

response = requests.post(
    'https://api.ugc.inc/media/social-audio',
    headers={
        'Authorization': 'Bearer YOUR_API_KEY',
        'Content-Type': 'application/json'
    },
    json={
        'tag': 'trending'
    }
)

data = response.json()

if data['ok']:
    for audio in data['data']:
        print(f"Audio: {audio['name']}")
        print(f"  Preview: {audio['preview_url']}")
        print(f"  Audio URL: {audio['audio_url']}")
```

```javascript JavaScript
const response = await fetch('https://api.ugc.inc/media/social-audio', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer YOUR_API_KEY',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    tag: 'trending'
  })
});

const data = await response.json();

if (data.ok) {
  data.data.forEach(audio => {
    console.log(`Audio: ${audio.name}`);
    console.log(`  TikTok Link: ${audio.social_audio_link}`);
  });
}
```

```typescript React
import { UGCClient } from 'ugcinc';

const client = new UGCClient({
  apiKey: 'YOUR_API_KEY'
});

// Get all social audio
const response = await client.media.getSocialAudio();

// Get by specific IDs
const byIds = await client.media.getSocialAudio({
  ids: ['audio_abc123', 'audio_def456']
});

// Get by tag
const byTag = await client.media.getSocialAudio({
  tag: 'trending'
});

if (response.ok) {
  response.data.forEach(audio => {
    console.log(`${audio.name} - ${audio.social_audio_link}`);
  });
}
```

</RequestExample>

<ResponseExample>

```json Success Response
{
  "ok": true,
  "code": 200,
  "data": [
    {
      "id": "audio_abc123",
      "org_id": "org_xyz789",
      "name": "Trending Sound - Artist",
      "tag": "trending",
      "social_post_link": "https://www.tiktok.com/@user/video/1234567890",
      "social_audio_link": "https://www.tiktok.com/music/Trending-Sound-1234567890",
      "platform_type": "tiktok",
      "preview_url": "https://api.ugc.inc/media/file/audio_abc123/preview",
      "audio_url": "https://api.ugc.inc/media/file/audio_abc123/audio",
      "created_at": "2024-01-15T10:30:00.000Z",
      "media_type": "social_audio",
      "type": "social_audio"
    }
  ]
}
```

</ResponseExample>

