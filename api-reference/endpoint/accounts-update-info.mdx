---
title: "Update Account Info"
description: "Update account metadata (tag, groups, keywords, profiles, description, warmup version)"
---

## Endpoint

```
POST https://api.ugc.inc/accounts/update-info
```

## Overview

Update account metadata including tag, organization group, user group, keywords, profiles, description, and warmup version. This endpoint updates the internal categorization and grouping of accounts without modifying the social media profile itself.

## Request Body

<ParamField body="accountId" type="string" required>
  Account ID to update
</ParamField>

<ParamField body="tag" type="string">
  New tag value for categorization
</ParamField>

<ParamField body="org_group" type="string">
  New organization group identifier
</ParamField>

<ParamField body="user_group" type="string">
  New user group identifier
</ParamField>

<ParamField body="keywords" type="string">
  Comma-separated list of keywords for warmup activities (e.g., "fitness,health,workout")
</ParamField>

<ParamField body="profiles" type="string">
  Comma-separated list of profile usernames for warmup activities (e.g., "@fitinfluencer1,@healthguru2")
</ParamField>

<ParamField body="description" type="string">
  Description/interest area for the account. Used by v1_smart warmup for browse tasks (e.g., "health and wellness content")
</ParamField>

<ParamField body="warmupVersion" type="'original' | 'v1_smart'">
  Warmup scheduling algorithm version. `'original'` uses standard scheduling (20-30 min durations), `'v1_smart'` uses optimized algorithm with custom flows (5-10 min after day 4)
</ParamField>

<ParamField body="postVersion" type="'original' | 'v1_custom'">
  Post flow version. `'original'` uses standard posting flow, `'v1_custom'` uses customized posting flow
</ParamField>

<Note>
  At least one of `tag`, `org_group`, `user_group`, `keywords`, `profiles`, `description`, `warmupVersion`, or `postVersion` must be provided.
</Note>

## Response

<ResponseField name="data" type="object">
  Response object containing success message and updated account
  
  <Expandable title="Response properties">
    <ResponseField name="message" type="string">
      Success message
    </ResponseField>
    
    <ResponseField name="account" type="Account">
      Updated account object with new values
    </ResponseField>
  </Expandable>
</ResponseField>

<RequestExample>

```bash cURL
curl -X POST https://api.ugc.inc/accounts/update-info \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "accountId": "acc_123456",
    "tag": "premium",
    "org_group": "marketing",
    "user_group": "team_alpha",
    "keywords": "fitness,health,workout",
    "profiles": "@fitinfluencer1,@healthguru2",
    "description": "health and wellness content",
    "warmupVersion": "v1_smart"
  }'
```

```python Python
import requests

response = requests.post(
    'https://api.ugc.inc/accounts/update-info',
    headers={
        'Authorization': 'Bearer YOUR_API_KEY',
        'Content-Type': 'application/json'
    },
    json={
        'accountId': 'acc_123456',
        'tag': 'premium',
        'org_group': 'marketing',
        'user_group': 'team_alpha',
        'keywords': 'fitness,health,workout',
        'profiles': '@fitinfluencer1,@healthguru2',
        'description': 'health and wellness content',
        'warmupVersion': 'v1_smart'
    }
)

data = response.json()

if data['ok']:
    print('Updated account:', data['data']['account'])
```

```javascript JavaScript
const response = await fetch('https://api.ugc.inc/accounts/update-info', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer YOUR_API_KEY',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    accountId: 'acc_123456',
    tag: 'premium',
    org_group: 'marketing',
    user_group: 'team_alpha',
    keywords: 'fitness,health,workout',
    profiles: '@fitinfluencer1,@healthguru2',
    description: 'health and wellness content',
    warmupVersion: 'v1_smart'
  })
});

const data = await response.json();

if (data.ok) {
  console.log('Updated account:', data.data.account);
}
```

```typescript React
import { UGCClient } from 'ugcinc';

const client = new UGCClient({
  apiKey: 'YOUR_API_KEY'
});

const response = await client.accounts.updateInfo({
  accountId: 'acc_123456',
  tag: 'premium',
  org_group: 'marketing',
  user_group: 'team_alpha',
  keywords: 'fitness,health,workout',
  profiles: '@fitinfluencer1,@healthguru2',
  description: 'health and wellness content',
  warmupVersion: 'v1_smart'
});

if (response.ok) {
  console.log('Account updated:', response.data.message);
  console.log('New tag:', response.data.account.tag);
}
```

</RequestExample>

<ResponseExample>

```json Success Response
{
  "ok": true,
  "code": 200,
  "message": "Account info updated successfully",
  "data": {
    "message": "Account info updated successfully",
    "account": {
      "id": "acc_123456",
      "type": "tiktok",
      "tag": "premium",
      "org_group": "marketing",
      "user_group": "team_alpha",
      "keywords": "fitness,health,workout",
      "profiles": "@fitinfluencer1,@healthguru2",
      "description": "health and wellness content",
      "warmup_version": "v1_smart",
      "username": "coolcreator",
      "nick_name": "Cool Creator",
      "pfp_url": "https://storage.example.com/avatar.jpg"
    }
  }
}
```

</ResponseExample>

