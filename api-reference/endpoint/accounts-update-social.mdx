---
title: "Update Account Social Profile"
description: "Update account social profile (avatar, nickname, bio)"
---

## Endpoint

```
POST https://api.ugc.inc/accounts/update-social
```

## Overview

Update the social media profile of an account including avatar, nickname (display name), and bio. This endpoint creates a task that will update the actual TikTok/Instagram profile.

<Warning>
  **Important Rate Limits:**
  - **Nickname:** Can only be updated **once every 7 days**
  - **Avatar:** Can only be updated **once every 24 hours**
  - **Bio:** Can only be updated **once every 24 hours**
  
  If you attempt to update a field before its cooldown period has elapsed, you'll receive an error with details about when the next update is allowed.
</Warning>

<Warning>
  **Avatar Image Requirements:**
  - **Aspect Ratio:** Avatar images must have a **1:1 aspect ratio** (square images)
  - **Minimum Dimensions:** At least **250x250 pixels**
  - **Supported Formats:** JPEG, PNG, WebP
  - **Accessibility:** URL must be publicly accessible
  
  If the image does not meet these requirements, the request will be rejected with an error showing the actual dimensions.
</Warning>

## Request Body

<ParamField body="accountId" type="string" required>
  Account ID to update
</ParamField>

<ParamField body="avatarUrl" type="string">
  New avatar/profile picture URL (must be publicly accessible, have a 1:1 aspect ratio, and be at least 250x250 pixels)
</ParamField>

<ParamField body="nickName" type="string">
  New display name (max 30 characters)
</ParamField>

<ParamField body="bio" type="string">
  New bio text (max 80 characters for TikTok, 150 for Instagram)
</ParamField>

<Note>
  At least one of `avatarUrl`, `nickName`, or `bio` must be provided.
</Note>

## Response

<ResponseField name="data" type="object">
  <Expandable title="Response properties">
    <ResponseField name="message" type="string">
      Success message indicating the profile update task was created
    </ResponseField>
  </Expandable>
</ResponseField>

<RequestExample>

```bash cURL
curl -X POST https://api.ugc.inc/accounts/update-social \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "accountId": "acc_123456",
    "nickName": "Cool Creator ✨",
    "bio": "Content creator 🎥 | Follow for daily tips",
    "avatarUrl": "https://storage.example.com/new-avatar.jpg"
  }'
```

```python Python
import requests

response = requests.post(
    'https://api.ugc.inc/accounts/update-social',
    headers={
        'Authorization': 'Bearer YOUR_API_KEY',
        'Content-Type': 'application/json'
    },
    json={
        'accountId': 'acc_123456',
        'nickName': 'Cool Creator ✨',
        'bio': 'Content creator 🎥 | Follow for daily tips',
        'avatarUrl': 'https://storage.example.com/new-avatar.jpg'
    }
)

data = response.json()

if data['ok']:
    print('Profile update scheduled:', data['data']['message'])
else:
    print('Error:', data['message'])
```

```javascript JavaScript
const response = await fetch('https://api.ugc.inc/accounts/update-social', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer YOUR_API_KEY',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    accountId: 'acc_123456',
    nickName: 'Cool Creator ✨',
    bio: 'Content creator 🎥 | Follow for daily tips',
    avatarUrl: 'https://storage.example.com/new-avatar.jpg'
  })
});

const data = await response.json();

if (data.ok) {
  console.log('Profile update scheduled:', data.data.message);
} else {
  console.error('Error:', data.message);
}
```

```typescript React
import { UGCClient } from 'ugcinc';

const client = new UGCClient({
  apiKey: 'YOUR_API_KEY'
});

const response = await client.accounts.updateSocial({
  accountId: 'acc_123456',
  nickName: 'Cool Creator ✨',
  bio: 'Content creator 🎥 | Follow for daily tips',
  avatarUrl: 'https://storage.example.com/new-avatar.jpg'
});

if (response.ok) {
  console.log('Success:', response.data.message);
} else {
  // Error includes when you can update again
  console.error('Error:', response.message);
}
```

</RequestExample>

<ResponseExample>

```json Success Response
{
  "ok": true,
  "code": 200,
  "message": "Profile update task created successfully",
  "data": {
    "message": "Profile update task created successfully"
  }
}
```

```json Rate Limit Error (Nickname)
{
  "ok": false,
  "code": 400,
  "message": "Nickname can only be updated once every 7 days. Last update was 3 days ago. You can update again in 4 day(s) on 2024-12-29"
}
```

```json Rate Limit Error (Avatar)
{
  "ok": false,
  "code": 400,
  "message": "Avatar can only be updated once every 24 hours. Last update was 12 hours ago. You can update again in 12 hour(s) on 2024-12-26"
}
```

```json Invalid Aspect Ratio Error
{
  "ok": false,
  "code": 400,
  "message": "Avatar image must have a 1:1 aspect ratio. Current image is 1920x1080 (1.78:1)"
}
```

```json Minimum Dimensions Error
{
  "ok": false,
  "code": 400,
  "message": "Avatar image must be at least 250x250 pixels. Current image is 100x100"
}
```

</ResponseExample>

