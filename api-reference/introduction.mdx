---
title: "Introduction"
description: "Official API documentation for UGC Inc - Automate TikTok and Instagram content creation"
---

## Welcome to UGC Inc API

The UGC Inc API allows you to programmatically manage social media accounts, create and schedule posts, track statistics, and automate account warming tasks for TikTok and Instagram.

## Base URL

All API requests should be made to:

```
https://api.ugc.inc
```

## Authentication

All API endpoints require authentication using an API key. Include your API key in the request headers:

```bash
Authorization: Bearer YOUR_API_KEY
```

### Getting Your API Key

1. Sign up or log in at [ugc.inc](https://ugc.inc)
2. Navigate to your dashboard
3. Generate an API key from the API settings

<Warning>
  **Keep your API key secure!** Never commit it to version control or expose it in client-side code.
</Warning>

## Installation

### NPM Package (Recommended)

We provide an official TypeScript/JavaScript client with full type safety:

```bash
npm install ugcinc
```

```typescript
import { UGCClient } from 'ugcinc';

const client = new UGCClient({
  apiKey: 'your-api-key-here'
});
```

### Direct API Calls

You can also make direct HTTP requests to the API endpoints using any HTTP client.

## Response Format

All API responses follow a consistent format:

### Success Response

```json
{
  "ok": true,
  "code": 200,
  "message": "Success",
  "data": { ... }
}
```

### Error Response

```json
{
  "ok": false,
  "code": 400,
  "message": "Error description"
}
```

## API Endpoints

The API is organized into the following categories:

### 📋 Accounts
Manage social media accounts, update profiles, and check account status.

### 📊 Statistics
Retrieve and refresh statistics for accounts and posts.

### ✅ Tasks
Manage and monitor tasks including warmup activities and profile updates.

### 📱 Posts
Create, schedule, and monitor video and slideshow posts.

## Rate Limiting

The API implements rate limiting to ensure fair usage:

- **Standard tier:** 100 requests per minute
- **Premium tier:** 500 requests per minute

Rate limit information is included in response headers:
- `X-RateLimit-Limit`: Maximum requests per minute
- `X-RateLimit-Remaining`: Remaining requests in current window
- `X-RateLimit-Reset`: Time when the rate limit resets (Unix timestamp)

## Support

Need help? Contact our support team:

- Email: support@ugc.inc
- Documentation: [ugc.inc/docs](https://ugc.inc/docs)
- Status Page: [status.ugc.inc](https://status.ugc.inc)

## SDKs and Libraries

- **JavaScript/TypeScript:** [npm package](https://www.npmjs.com/package/ugcinc)
- **Python:** Coming soon
- **Go:** Coming soon
